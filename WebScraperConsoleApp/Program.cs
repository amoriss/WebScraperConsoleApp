
using HtmlAgilityPack;
using WebScraperConsoleApp;

BentonAloe.ViewPrices();

// HttpClient client = new HttpClient();
// string websiteURL = $"https://developer.mozilla.org/en-US/docs/WebAssembly";
//
// string response = client.GetStringAsync(websiteURL).Result;
//
// HtmlDocument htmlDocument = new HtmlDocument();
//
// htmlDocument.LoadHtml(response);
//
// HtmlNodeCollection h2TitleNodes = htmlDocument.DocumentNode.SelectNodes("//h2");
//
// string[] h2Titles = new string[h2TitleNodes.Count];
//
// for (int i = 0; i < h2TitleNodes.Count; i++)
// {
//     h2Titles[i] = h2TitleNodes[i].InnerText.Trim();
// }
//
// foreach (var title in h2Titles)
// {
//     Console.WriteLine(title);
// }
